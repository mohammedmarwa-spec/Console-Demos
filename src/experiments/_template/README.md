# Experiment template

Copy this folder to `src/experiments/<owner>/<experiment-name>/` or run:

```bash
node scripts/create-experiment.mjs --owner <name> --name <slug> --from <scenarioId>
```

Then register the experiment in `src/registry/experiments.ts`.
