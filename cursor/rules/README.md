# Project rules

All `.md` files in this folder are treated as **project rules**. The AI will read and apply their guidelines when working in this project.

Add rule files here as needed, for example:

- `conventions.md` – coding style and conventions
- `architecture.md` – structure and patterns
- `testing.md` – how to write and run tests

Keep each file focused and under ~50 lines when possible.
